export interface Site {
    $key: string;
    initials: string;
    nameEN: string;
    nameTH: string;
    productId: string;
    maLevelId: string;
}

export interface MaLevel {
    id: string;
    name: string;
}